#!/usr/bin/awk
# DS:FIT
#
# Stub only — not runnable. Prefer `ds:fit`, which loads:
#   support/utils.awk
#   support/wcwidth.awk   (when awksafe)
#   scripts/fit_columns_functions.awk
#   scripts/fit_columns_program.awk
#
# Help text: scripts/fit_columns_documentation.awk (`ds:fit -h`).
#
# Do not -f this file together with the modular sources (no BEGIN here).
