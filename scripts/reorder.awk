#!/usr/bin/awk
# DS:REO
#
# Stub only — not runnable. Prefer `ds:reo`, which loads:
#   support/utils.awk
#   scripts/reorder_functions.awk
#   scripts/reorder_program.awk
#
# Help text: scripts/reorder_documentation.awk (`ds:reo -h`).
#
# Do not -f this file together with the modular sources (no BEGIN here).
